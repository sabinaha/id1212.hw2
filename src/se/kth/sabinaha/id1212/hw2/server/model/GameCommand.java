package se.kth.sabinaha.id1212.hw2.server.model;

/**
 * Enum for the different types of command that can be parsed by the game logic
 */
public enum GameCommand {
    MAKE_GUESS, FETCH_INFO, START_GAME, INVALID_COMMAND, EXIT, RESTART
}
